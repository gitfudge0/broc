{
  "name": "browser-control",
  "version": "0.1.0",
  "description": "Browser extension for AI agent browser control via native messaging (Firefox + Chrome)",
  "private": true,
  "type": "module",
  "bin": {
    "browser-control": "dist/cli.mjs"
  },
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "test": "vitest run",
    "build": "node build.mjs",
    "clean": "node scripts/clean.mjs",
    "setup": "node scripts/repo-cli.mjs setup",
    "launch": "node scripts/repo-cli.mjs launch"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.7.0",
    "vitest": "^4.0.18",
    "web-ext": "^8.4.0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.27.1",
    "@puppeteer/browsers": "^2.13.0"
  }
}
